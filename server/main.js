import { Meteor } from 'meteor/meteor';
import Links from '/imports/api/lines';
import Feedbacks from '/imports/api/feedback'
Meteor.startup(() => {
  // If the Links collection is empty, add some data.

});
